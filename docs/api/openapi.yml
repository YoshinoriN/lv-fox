openapi: 3.0.1
info:
  title: lv-fox API
  description: |
    # Description
    This API documentation regarding OpenAPI specification. Generated by [ReDoc](https://github.com/Rebilly/ReDoc)
  version: 0.0.1
externalDocs:
  description: Source code
  url: 'https://github.com/yoshinorin/lv-fox'
servers:
  - url: http://example.com/
paths:
  /status:
    get:
      summary: Get API server status
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: operational
  /search:
    get:
      summary: Search post
      responses:
        200:
          description: Response when Success
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/page'
        401:
          description: Unauthorized
        422:
          description: Unprocess request
      parameters:
        - in: q
          name: query
          schema:
            type: string
          description: Word for seach post content.
  /page:
    post:
      summary: Create a post
      security:
        - bearer:
          - write
      responses:
        201:
          description: Create a comment succeeded
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/page'
        400:
          description: Payload is wrong
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/errorMessage'
        401:
          description: Unauthorized
        422:
          description: Unprocess request

components:
  schemas:
    page:
      type: object
      properties:
        url:
          type: string
          example: https://example.com
        title:
          type: string
          example: "Hello lv-fox!!"
        content:
          type: string
          example: "lv-fox is search API server for my blog......."
        publishedAt:
          type: long
          description: Unix time
          example: 1567814290
        updatedAt:
          type: long
          description: Unix time
          example: 1567818890
    errorMessage:
      type: object
      properties:
        message:
          type: string
          example: Something is wrong
    errorMessageNotFound:
      type: object
      properties:
        message:
          type: string
          example: NotFound
  securitySchemes:
    bearer:
      type: http
      scheme: bearer
      format: JWT
      description: |
        lv-fox read auth token from environment variables.
      flows:
        authorizationCode:
          scopes:
            write:
            read:
